export enum ProductType {
  /**
   * define that it's a product that can be purchased
   */
  SINGLE = 'single',

  /**
   * define that it's a subscription product that can be purchased
   */
  SUBSCRIPTION = 'subscription',
}
